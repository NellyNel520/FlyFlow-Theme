{% comment %}
  FlyFlow Theme - Main Product Section
  Purpose: Block-based OS 2.0 product template with gallery, variant selectors,
           inventory messaging, app blocks, and dynamic checkout support.
  Dependencies: theme.js (VariantSelector, ProductGallery, StickyATC, Cart modules)
  Last modified: 2026-02-18
{% endcomment %}

<section class="section main-product-section" data-section-id="{{ section.id }}" style="--pdp-sticky-top: {{ section.settings.sticky_top_offset | default: 80 }}px;">
  <div class="container">
    {% if section.settings.show_product_breadcrumb %}
      <nav class="breadcrumbs mb-lg" aria-label="Breadcrumb" style="font-size:0.8125rem;color:var(--color-text-secondary)">
        <a href="{{ routes.root_url }}">Home</a>
        <span aria-hidden="true">/</span>
        {% if product.collections.size > 0 %}
          <a href="{{ product.collections.first.url }}">{{ product.collections.first.title }}</a>
          <span aria-hidden="true">/</span>
        {% endif %}
        <span aria-current="page">{{ product.title }}</span>
      </nav>
    {% endif %}

    <div class="product">
      {% render 'product-gallery', product: product, parent_section_id: section.id %}

      <div class="product-info">
        {% assign has_product_app_block = false %}
        {% for section_block in section.blocks %}
          {% if section_block.type == '@app' %}
            {% assign has_product_app_block = true %}
          {% endif %}
        {% endfor %}

        {% form 'product', product, data-product-form: '' %}
          <input type="hidden" name="id" value="{{ product.selected_or_first_available_variant.id }}">
          {% assign purchase_panel_types = 'variant_picker,quantity_selector,buy_buttons' | split: ',' %}
          {% assign purchase_panel_open = false %}

          {% for block in section.blocks %}
            {% if block.type == 'tab_spec' or block.type == 'reviews_faq' %}
              {% continue %}
            {% endif %}

            {% assign is_purchase_block = false %}
            {% if purchase_panel_types contains block.type %}
              {% assign is_purchase_block = true %}
            {% endif %}

            {% assign next_index = forloop.index0 | plus: 1 %}
            {% assign next_block_type = '' %}
            {% if next_index < section.blocks.size %}
              {% assign next_block_type = section.blocks[next_index].type %}
            {% endif %}
            {% assign next_is_purchase_block = false %}
            {% if purchase_panel_types contains next_block_type %}
              {% assign next_is_purchase_block = true %}
            {% endif %}

            {% if is_purchase_block and purchase_panel_open == false %}
              <div class="product-purchase-panel">
              {% assign purchase_panel_open = true %}
            {% endif %}

            <div {% if block.type != '@app' %}class="product-block product-block--{{ block.type | replace: '_', '-' }}"{% endif %} {{ block.shopify_attributes }}>
              {% case block.type %}
                {% when 'vendor' %}
                  {% if product.vendor != blank %}
                    <p class="product-info__vendor">{{ product.vendor }}</p>
                  {% endif %}

                {% when 'title' %}
                  <h1 class="product-info__title">{{ product.title }}</h1>

                {% when 'price' %}
                  <div class="product-info__price{% if product.compare_at_price > product.price %} product-info__price--sale{% endif %}">
                    <span class="product-info__price-current" data-product-price>
                      {{ product.selected_or_first_available_variant.price | money }}
                    </span>
                    <span
                      class="product-info__price-compare"
                      data-product-compare-price
                      {% unless product.compare_at_price > product.price %}style="display:none"{% endunless %}
                    >
                      {{ product.compare_at_price | money }}
                    </span>
                  </div>

                {% when 'rating' %}
                  {% assign rating_source = settings.rating_source | default: 'auto' %}
                  {% assign rating_value = product.metafields.reviews.rating.value.rating | default: product.metafields.reviews.rating.value %}
                  {% assign rating_count = product.metafields.reviews.rating_count | default: product.metafields.spr.reviews %}
                  {% assign show_metafield_rating = false %}

                  {% if settings.show_star_rating %}
                    {% if rating_source == 'metafield_only' %}
                      {% if rating_value != blank %}{% assign show_metafield_rating = true %}{% endif %}
                    {% elsif rating_source == 'app_only' %}
                      {% assign show_metafield_rating = false %}
                    {% else %}
                      {% if has_product_app_block %}
                        {% assign show_metafield_rating = false %}
                      {% elsif rating_value != blank %}
                        {% assign show_metafield_rating = true %}
                      {% endif %}
                    {% endif %}
                  {% endif %}

                  {% if show_metafield_rating %}
                    {% assign rating_value_number = rating_value | plus: 0 %}
                    {% assign rating_percentage = rating_value_number | times: 100.0 | divided_by: 5 %}
                    {% assign rating_clip_id = 'rating-clip-' | append: section.id | append: '-' | append: block.id %}
                    <div class="product-info__rating" aria-label="Rated {{ rating_value_number }} out of 5">
                      <svg class="product-info__rating-svg" viewBox="0 0 120 24" aria-hidden="true" role="img">
                        <defs>
                          <clipPath id="{{ rating_clip_id }}">
                            <rect x="0" y="0" width="{{ rating_percentage }}%" height="100%"></rect>
                          </clipPath>
                        </defs>
                        <g class="product-info__rating-stars-base">
                          <path d="M12 2.5l2.8 5.6 6.2.9-4.5 4.3 1.1 6.1L12 16.6 6.4 19.4l1.1-6.1L3 9l6.2-.9L12 2.5z"></path>
                          <path transform="translate(24 0)" d="M12 2.5l2.8 5.6 6.2.9-4.5 4.3 1.1 6.1L12 16.6 6.4 19.4l1.1-6.1L3 9l6.2-.9L12 2.5z"></path>
                          <path transform="translate(48 0)" d="M12 2.5l2.8 5.6 6.2.9-4.5 4.3 1.1 6.1L12 16.6 6.4 19.4l1.1-6.1L3 9l6.2-.9L12 2.5z"></path>
                          <path transform="translate(72 0)" d="M12 2.5l2.8 5.6 6.2.9-4.5 4.3 1.1 6.1L12 16.6 6.4 19.4l1.1-6.1L3 9l6.2-.9L12 2.5z"></path>
                          <path transform="translate(96 0)" d="M12 2.5l2.8 5.6 6.2.9-4.5 4.3 1.1 6.1L12 16.6 6.4 19.4l1.1-6.1L3 9l6.2-.9L12 2.5z"></path>
                        </g>
                        <g class="product-info__rating-stars-fill" clip-path="url(#{{ rating_clip_id }})">
                          <path d="M12 2.5l2.8 5.6 6.2.9-4.5 4.3 1.1 6.1L12 16.6 6.4 19.4l1.1-6.1L3 9l6.2-.9L12 2.5z"></path>
                          <path transform="translate(24 0)" d="M12 2.5l2.8 5.6 6.2.9-4.5 4.3 1.1 6.1L12 16.6 6.4 19.4l1.1-6.1L3 9l6.2-.9L12 2.5z"></path>
                          <path transform="translate(48 0)" d="M12 2.5l2.8 5.6 6.2.9-4.5 4.3 1.1 6.1L12 16.6 6.4 19.4l1.1-6.1L3 9l6.2-.9L12 2.5z"></path>
                          <path transform="translate(72 0)" d="M12 2.5l2.8 5.6 6.2.9-4.5 4.3 1.1 6.1L12 16.6 6.4 19.4l1.1-6.1L3 9l6.2-.9L12 2.5z"></path>
                          <path transform="translate(96 0)" d="M12 2.5l2.8 5.6 6.2.9-4.5 4.3 1.1 6.1L12 16.6 6.4 19.4l1.1-6.1L3 9l6.2-.9L12 2.5z"></path>
                        </g>
                      </svg>
                      <span class="sr-only">Rated {{ rating_value_number }} out of 5</span>
                      <span class="product-info__rating-text">
                        {{ rating_value_number }}/5{% if settings.show_rating_count and rating_count != blank %} ({{ rating_count }}){% endif %}
                      </span>
                    </div>
                  {% endif %}

                {% when 'inventory' %}
                  {% unless section.settings.enable_low_stock_notice %}
                    {% assign current_variant = product.selected_or_first_available_variant %}
                    <div
                      class="stock-indicator{% if current_variant.available == false %} stock-indicator--out-of-stock{% elsif current_variant.inventory_management and current_variant.inventory_quantity > 0 and current_variant.inventory_quantity <= settings.product_stock_threshold %} stock-indicator--low-stock{% else %} stock-indicator--in-stock{% endif %}"
                      data-stock-indicator
                      data-threshold="{{ settings.product_stock_threshold }}"
                    >
                      <span class="stock-indicator__dot"></span>
                      {% if current_variant.available == false %}
                        {{ 'products.stock.out_of_stock' | t }}
                      {% elsif current_variant.inventory_management and current_variant.inventory_quantity > 0 and current_variant.inventory_quantity <= settings.product_stock_threshold %}
                        {{ 'products.stock.low_stock' | t: count: current_variant.inventory_quantity }}
                      {% else %}
                        {{ 'products.stock.in_stock' | t }}
                      {% endif %}
                    </div>
                  {% endunless %}

                {% when 'variant_picker' %}
                  {% for option in product.options_with_values %}
                    <div class="variant-selector" data-option-group>
                      <div class="variant-selector__label">
                        <span>{{ option.name }}:</span>
                        <span class="variant-selector__selected" data-option-selected>
                          {{ option.selected_value }}
                        </span>
                      </div>

                      <div class="variant-selector__options">
                        {% for value in option.values %}
                          {% assign found_available = false %}
                          {% for variant in product.variants %}
                            {% if variant.available %}
                              {% case option.position %}
                                {% when 1 %}
                                  {% if variant.option1 == value %}{% assign found_available = true %}{% endif %}
                                {% when 2 %}
                                  {% if variant.option2 == value %}{% assign found_available = true %}{% endif %}
                                {% when 3 %}
                                  {% if variant.option3 == value %}{% assign found_available = true %}{% endif %}
                              {% endcase %}
                            {% endif %}
                          {% endfor %}

                          {% assign option_name_downcase = option.name | downcase %}
                          {% if option_name_downcase contains 'color' or option_name_downcase contains 'colour' %}
                            {% capture swatch_color %}{% render 'swatch-color', value: value %}{% endcapture %}
                            <button
                              type="button"
                              class="swatch{% if option.selected_value == value %} swatch--active{% endif %}{% unless found_available %} swatch--sold-out{% endunless %}"
                              style="--swatch-color: {{ swatch_color | strip }}; background-color: {{ swatch_color | strip }};"
                              data-swatch-name="{{ value | escape }}"
                              data-option-value="{{ value }}"
                              data-option-name="{{ option.name }}"
                              title="{{ value }}{% unless found_available %} - {{ 'products.sold_out' | t }}{% endunless %}"
                              aria-label="{{ value }}{% unless found_available %} - {{ 'products.sold_out' | t }}{% endunless %}"
                              {% unless found_available %}aria-disabled="true"{% endunless %}
                            ></button>
                          {% else %}
                            <button
                              type="button"
                              class="size-btn{% if option.selected_value == value %} size-btn--active{% endif %}{% unless found_available %} size-btn--sold-out{% endunless %}"
                              data-option-value="{{ value }}"
                              data-option-name="{{ option.name }}"
                              aria-label="{{ value }}{% unless found_available %} - {{ 'products.sold_out' | t }}{% endunless %}"
                              {% unless found_available %}aria-disabled="true"{% endunless %}
                            >
                              {{ value }}
                            </button>
                          {% endif %}
                        {% endfor %}
                      </div>

                      {% if option.name == 'Size' and block.settings.show_size_guide %}
                        <button
                          class="text-accent text-sm mt-sm variant-selector__size-guide-trigger"
                          data-modal-open="size-guide-modal"
                          type="button"
                          style="background:none;border:none;cursor:pointer;text-decoration:underline;text-underline-offset:2px"
                        >
                          {{ 'products.tabs.size_guide' | t }}
                        </button>
                      {% endif %}
                    </div>
                  {% endfor %}

                {% when 'quantity_selector' %}
                  <div class="quantity-selector">
                    <button type="button" class="quantity-selector__btn" data-qty-minus aria-label="{{ 'products.decrease_quantity' | t }}">
                      &minus;
                    </button>
                    <input
                      type="number"
                      class="quantity-selector__input"
                      name="quantity"
                      value="1"
                      min="1"
                      aria-label="{{ 'products.quantity' | t }}"
                    >
                    <button type="button" class="quantity-selector__btn" data-qty-plus aria-label="{{ 'products.increase_quantity' | t }}">
                      &plus;
                    </button>
                  </div>

                {% when 'buy_buttons' %}
                  {% assign current_variant = product.selected_or_first_available_variant %}
                  {% assign low_stock_threshold = section.settings.low_stock_threshold | default: 12 %}
                  {% assign low_stock_bar_max = section.settings.low_stock_bar_max | default: 30 %}
                  {% assign low_stock_qty = current_variant.inventory_quantity | default: -1 %}
                  {% assign low_stock_tracked = false %}
                  {% if current_variant.inventory_management != blank %}
                    {% assign low_stock_tracked = true %}
                  {% endif %}
                  {% assign low_stock_known = false %}
                  {% if low_stock_qty >= 0 %}
                    {% assign low_stock_known = true %}
                  {% endif %}
                  {% assign low_stock_continue_with_no_stock = false %}
                  {% if current_variant.inventory_policy == 'continue' and low_stock_qty <= 0 %}
                    {% assign low_stock_continue_with_no_stock = true %}
                  {% endif %}
                  {% assign show_low_stock_notice = false %}
                  {% assign low_stock_in_stock_state = false %}
                  {% if section.settings.enable_low_stock_notice and low_stock_tracked and low_stock_known and current_variant.available and low_stock_continue_with_no_stock == false %}
                    {% if low_stock_qty > 0 and low_stock_qty <= low_stock_threshold %}
                      {% assign show_low_stock_notice = true %}
                    {% elsif section.settings.show_when_in_stock and low_stock_qty > low_stock_threshold %}
                      {% assign show_low_stock_notice = true %}
                      {% assign low_stock_in_stock_state = true %}
                    {% endif %}
                  {% endif %}
                  {% assign low_stock_fill = low_stock_qty | times: 100.0 | divided_by: low_stock_bar_max %}
                  {% if low_stock_fill < 0 %}
                    {% assign low_stock_fill = 0 %}
                  {% endif %}
                  {% if low_stock_fill > 100 %}
                    {% assign low_stock_fill = 100 %}
                  {% endif %}
                  {% assign low_stock_message_template = section.settings.low_stock_message | default: 'Hurry, only {{count}} items left in stock!' %}
                  {% assign low_stock_message_text = low_stock_message_template | replace: '{{ count }}', low_stock_qty | replace: '{{count}}', low_stock_qty | replace: '{count}', low_stock_qty %}
                  {% assign low_stock_has_count_token = false %}
                  {% if low_stock_message_template contains '{{count}}' or low_stock_message_template contains '{{ count }}' or low_stock_message_template contains '{count}' %}
                    {% assign low_stock_has_count_token = true %}
                  {% endif %}
                  {% if low_stock_has_count_token == false %}
                    {% assign low_stock_message_text = 'Hurry, only ' | append: low_stock_qty | append: ' items left in stock!' %}
                  {% endif %}

                  {% if section.settings.enable_low_stock_notice %}
                    <div
                      class="low-stock{% if low_stock_in_stock_state %} low-stock--in-stock{% endif %}{% if low_stock_qty <= 3 %} low-stock--critical{% endif %}"
                      data-low-stock
                      data-threshold="{{ low_stock_threshold }}"
                      data-bar-max="{{ low_stock_bar_max }}"
                      data-show-when-in-stock="{{ section.settings.show_when_in_stock }}"
                      data-message-template="{{ low_stock_message_template | escape }}"
                      data-in-stock-label="In stock"
                      {% unless show_low_stock_notice %}hidden{% endunless %}
                    >
                      <p class="low-stock__text" data-low-stock-text>
                        {% if low_stock_in_stock_state %}
                          In stock
                        {% else %}
                          {{ low_stock_message_text }}
                        {% endif %}
                      </p>
                      <div class="low-stock__bar" aria-hidden="true">
                        <span class="low-stock__bar-fill" data-low-stock-fill style="width: {{ low_stock_fill }}%"></span>
                      </div>
                    </div>
                  {% endif %}

                  <div class="product-info__add-to-cart" data-add-to-cart-section>
                    <button
                      type="submit"
                      name="add"
                      class="btn btn--primary btn--lg"
                      data-add-to-cart
                      data-add-text="{{ 'products.add_to_cart' | t }}"
                      data-sold-out-text="{{ 'products.sold_out' | t }}"
                      {% unless product.selected_or_first_available_variant.available %}disabled{% endunless %}
                    >
                      {% if product.selected_or_first_available_variant.available %}
                        {{ 'products.add_to_cart' | t }}
                      {% else %}
                        {{ 'products.sold_out' | t }}
                      {% endif %}
                    </button>

                    {% if block.settings.show_dynamic_checkout %}
                      <div class="product-info__dynamic-checkout">
                        {{ form | payment_button }}
                      </div>
                    {% endif %}
                  </div>

                {% when 'product_share' %}
                  {% if settings.show_product_share %}
                    {% render 'product-share', product: product, current_variant: product.selected_or_first_available_variant, section_id: section.id, block_id: block.id %}
                  {% endif %}

                {% when 'description' %}
                  {% if section.settings.enable_product_tabs == false and product.description != blank %}
                    <div class="product-info__description text-sm" style="color:var(--color-text-secondary);line-height:1.7">
                      {{ product.description }}
                    </div>
                  {% endif %}

                {% when 'back_in_stock' %}
                  {% unless product.available %}
                    <div class="back-in-stock" style="padding:var(--space-md);background:var(--color-surface);border-radius:var(--border-radius)">
                      <p class="text-sm" style="font-weight:600;margin-bottom:var(--space-sm)">
                        {{ 'products.stock.back_in_stock_title' | t }}
                      </p>
                      <div class="flex flex--gap-sm">
                        <input
                          type="email"
                          class="form__input"
                          placeholder="your@email.com"
                          required
                          aria-label="Email for back-in-stock notification"
                        >
                        <button type="button" class="btn btn--secondary btn--sm">
                          {{ 'products.stock.back_in_stock_button' | t }}
                        </button>
                      </div>
                    </div>
                  {% endunless %}

                {% when '@app' %}
                  {% unless section.settings.enable_product_tabs and section.settings.show_reviews_tab %}
                    {% render block %}
                  {% endunless %}
              {% endcase %}
            </div>

            {% if is_purchase_block and next_is_purchase_block == false and purchase_panel_open %}
              </div>
              {% assign purchase_panel_open = false %}
            {% endif %}
          {% endfor %}

          {% if purchase_panel_open %}
            </div>
          {% endif %}
        {% endform %}
      </div>
    </div>

    {% if section.settings.enable_product_tabs %}
      {% render 'product-tabs', product: product, section: section %}
    {% endif %}
  </div>

  <script type="application/json" data-product-json>
    {{ product | json }}
  </script>

  {% render 'size-guide-modal' %}
  <script src="{{ 'product-tabs.js' | asset_url }}" defer></script>

  {% if settings.product_sticky_atc %}
    <div class="sticky-atc" data-sticky-atc>
      <div class="sticky-atc__inner container">
        <div class="sticky-atc__info">
          <span class="sticky-atc__title">{{ product.title }}</span>
          <span class="sticky-atc__price" data-product-price>
            {{ product.selected_or_first_available_variant.price | money }}
          </span>
        </div>
        <button
          class="btn btn--primary"
          data-add-to-cart
          data-variant-id="{{ product.selected_or_first_available_variant.id }}"
          data-add-text="{{ 'products.add_to_cart' | t }}"
          data-sold-out-text="{{ 'products.sold_out' | t }}"
          {% unless product.selected_or_first_available_variant.available %}disabled{% endunless %}
        >
          {% if product.selected_or_first_available_variant.available %}
            {{ 'products.add_to_cart' | t }}
          {% else %}
            {{ 'products.sold_out' | t }}
          {% endif %}
        </button>
      </div>
    </div>
  {% endif %}
</section>

{% schema %}
{
  "name": "Main Product",
  "settings": [
    {
      "type": "range",
      "id": "sticky_top_offset",
      "label": "Sticky info top offset",
      "min": 56,
      "max": 140,
      "step": 4,
      "default": 80,
      "unit": "px"
    },
    {
      "type": "checkbox",
      "id": "show_product_breadcrumb",
      "label": "Show product breadcrumb",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "enable_low_stock_notice",
      "label": "Enable low stock notice",
      "default": true
    },
    {
      "type": "range",
      "id": "low_stock_threshold",
      "label": "Low stock threshold",
      "min": 1,
      "max": 30,
      "step": 1,
      "default": 12
    },
    {
      "type": "text",
      "id": "low_stock_message",
      "label": "Low stock message",
      "default": "Hurry, only {{count}} items left in stock!"
    },
    {
      "type": "range",
      "id": "low_stock_bar_max",
      "label": "Low stock bar max quantity",
      "min": 10,
      "max": 100,
      "step": 1,
      "default": 30
    },
    {
      "type": "checkbox",
      "id": "show_when_in_stock",
      "label": "Show notice when stock is above threshold",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "enable_product_tabs",
      "label": "Enable product tabs",
      "default": true
    },
    {
      "type": "select",
      "id": "tabs_style",
      "label": "Mobile tabs style",
      "default": "tabs",
      "options": [
        { "value": "tabs", "label": "Horizontal tabs" },
        { "value": "accordion", "label": "Accordion style" }
      ]
    },
    {
      "type": "checkbox",
      "id": "show_details_tab",
      "label": "Show Details tab",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_size_guide_tab",
      "label": "Show Size Guide tab",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_reviews_tab",
      "label": "Show Reviews tab",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_shipping_returns_tab",
      "label": "Show Shipping & Returns tab",
      "default": true
    },
    {
      "type": "select",
      "id": "initial_active_tab",
      "label": "Initial active tab",
      "default": "details",
      "options": [
        { "value": "details", "label": "Details" },
        { "value": "size", "label": "Size Guide" },
        { "value": "reviews", "label": "Reviews" },
        { "value": "shipping", "label": "Shipping & Returns" }
      ]
    },
    {
      "type": "richtext",
      "id": "details_intro_richtext",
      "label": "Details intro (fallback)",
      "default": "<p>This piece is designed with modern proportion, clean structure, and everyday versatility in mind. Update this copy to match your product story.</p>"
    },
    {
      "type": "textarea",
      "id": "default_highlights",
      "label": "Default highlights (one per line)",
      "default": "Premium construction\\nSoft hand feel\\nDesigned for daily wear\\nBalanced modern fit"
    },
    {
      "type": "richtext",
      "id": "details_care_richtext",
      "label": "Care fallback",
      "default": "<p>Follow the care instructions on the garment label for best results and long-term wear.</p>"
    },
    {
      "type": "richtext",
      "id": "details_fit_richtext",
      "label": "Fit fallback",
      "default": "<p>Fit may vary by style and preference. Compare measurements to your everyday favorites for guidance.</p>"
    },
    {
      "type": "page",
      "id": "size_guide_default_page",
      "label": "Default size guide page"
    },
    {
      "type": "richtext",
      "id": "size_guide_intro_richtext",
      "label": "Size guide intro",
      "default": "<p>Use this guide to compare your measurements with our size chart before ordering.</p>"
    },
    {
      "type": "textarea",
      "id": "size_guide_measurement_steps",
      "label": "How to measure steps (one per line)",
      "default": "Measure your chest at the fullest point\\nMeasure your waist at the natural waistline\\nCompare your measurements with the table below"
    },
    {
      "type": "image_picker",
      "id": "size_guide_diagram_image",
      "label": "Size guide diagram image"
    },
    {
      "type": "richtext",
      "id": "size_guide_fit_notes_richtext",
      "label": "Size guide fit notes",
      "default": "<p>If you prefer a looser silhouette, consider sizing up. For a closer fit, choose your regular size.</p>"
    },
    {
      "type": "url",
      "id": "size_guide_help_link",
      "label": "Size guide help link"
    },
    {
      "type": "text",
      "id": "size_guide_help_label",
      "label": "Size guide help label",
      "default": "Need help choosing a size?"
    },
    {
      "type": "text",
      "id": "reviews_empty_state_title",
      "label": "Reviews empty state title",
      "default": "Reviews Coming Soon"
    },
    {
      "type": "richtext",
      "id": "reviews_empty_state_body_richtext",
      "label": "Reviews empty state body",
      "default": "<p>Customer reviews will appear here once published. Install and connect your preferred reviews app to start collecting feedback.</p>"
    },
    {
      "type": "richtext",
      "id": "shipping_returns_intro_richtext",
      "label": "Shipping & returns intro",
      "default": "<p>Shipping options and delivery estimates are shown at checkout. Returns are handled according to your store policy settings.</p>"
    },
    {
      "type": "text",
      "id": "shipping_page_handle",
      "label": "Shipping page handle",
      "info": "Optional. Example: shipping-policy"
    },
    {
      "type": "text",
      "id": "returns_page_handle",
      "label": "Returns page handle",
      "info": "Optional. Example: returns-policy"
    },
    {
      "type": "richtext",
      "id": "shipping_richtext",
      "label": "Shipping fallback content",
      "default": "<p>Shipping options are calculated at checkout based on destination and selected service.</p>"
    },
    {
      "type": "richtext",
      "id": "returns_richtext",
      "label": "Returns fallback content",
      "default": "<p>Returns are accepted according to your return policy. Review your policy page for full details.</p>"
    },
    {
      "type": "richtext",
      "id": "order_processing_note_richtext",
      "label": "Order processing note",
      "default": "<p>Order processing times may vary during high-volume periods.</p>"
    },
    {
      "type": "url",
      "id": "faq_link",
      "label": "FAQ custom link"
    },
    {
      "type": "text",
      "id": "faq_page_handle",
      "label": "FAQ page handle",
      "info": "Optional. Used before FAQ custom link."
    },
    {
      "type": "text",
      "id": "faq_link_label",
      "label": "FAQ link label",
      "default": "Read FAQ"
    },
    {
      "type": "select",
      "id": "return_policy_link_source",
      "label": "Return policy link source",
      "default": "shop_policy",
      "options": [
        { "value": "shop_policy", "label": "Shop refund policy" },
        { "value": "page", "label": "Page handle" },
        { "value": "custom_url", "label": "Custom URL" }
      ]
    },
    {
      "type": "text",
      "id": "return_policy_page_handle",
      "label": "Return policy page handle",
      "info": "Used when source is Page handle."
    },
    {
      "type": "url",
      "id": "return_policy_custom_url",
      "label": "Return policy custom URL",
      "info": "Used when source is Custom URL."
    },
    {
      "type": "text",
      "id": "return_policy_link_label",
      "label": "Return policy link label",
      "default": "View return policy"
    }
  ],
  "blocks": [
    {
      "type": "vendor",
      "name": "Vendor",
      "limit": 1
    },
    {
      "type": "title",
      "name": "Title",
      "limit": 1
    },
    {
      "type": "price",
      "name": "Price",
      "limit": 1
    },
    {
      "type": "rating",
      "name": "Rating",
      "limit": 1
    },
    {
      "type": "inventory",
      "name": "Inventory status",
      "limit": 1
    },
    {
      "type": "variant_picker",
      "name": "Variant picker",
      "limit": 1,
      "settings": [
        {
          "type": "checkbox",
          "id": "show_size_guide",
          "label": "Show size guide link",
          "default": true
        }
      ]
    },
    {
      "type": "quantity_selector",
      "name": "Quantity selector",
      "limit": 1
    },
    {
      "type": "buy_buttons",
      "name": "Buy buttons",
      "limit": 1,
      "settings": [
        {
          "type": "checkbox",
          "id": "show_dynamic_checkout",
          "label": "Show dynamic checkout buttons",
          "default": true
        }
      ]
    },
    {
      "type": "product_share",
      "name": "Product share",
      "limit": 1
    },
    {
      "type": "description",
      "name": "Description",
      "limit": 1
    },
    {
      "type": "back_in_stock",
      "name": "Back in stock",
      "limit": 1
    },
    {
      "type": "tab_spec",
      "name": "Tab spec row",
      "settings": [
        {
          "type": "text",
          "id": "label",
          "label": "Label",
          "default": "Material"
        },
        {
          "type": "text",
          "id": "value",
          "label": "Value",
          "default": "Update in theme editor"
        }
      ]
    },
    {
      "type": "reviews_faq",
      "name": "Reviews FAQ",
      "settings": [
        {
          "type": "text",
          "id": "question",
          "label": "Question",
          "default": "How are reviews verified?"
        },
        {
          "type": "richtext",
          "id": "answer",
          "label": "Answer",
          "default": "<p>Verification depends on your selected reviews app settings.</p>"
        }
      ]
    },
    {
      "type": "@app"
    }
  ],
  "presets": [
    {
      "name": "Main Product",
      "blocks": [
        { "type": "vendor" },
        { "type": "title" },
        { "type": "price" },
        { "type": "rating" },
        { "type": "inventory" },
        { "type": "variant_picker" },
        { "type": "quantity_selector" },
        { "type": "buy_buttons" },
        { "type": "product_share" },
        { "type": "description" },
        {
          "type": "tab_spec",
          "settings": { "label": "Weight", "value": "Lightweight build for everyday wear" }
        },
        {
          "type": "tab_spec",
          "settings": { "label": "Origin", "value": "Refer to garment label for origin details" }
        },
        {
          "type": "tab_spec",
          "settings": { "label": "Model Info", "value": "Model sizing details can be added here" }
        },
        {
          "type": "reviews_faq",
          "settings": {
            "question": "How are reviews verified?",
            "answer": "<p>Verification depends on your selected reviews app settings and workflows.</p>"
          }
        },
        {
          "type": "reviews_faq",
          "settings": {
            "question": "When will reviews appear?",
            "answer": "<p>Reviews appear after your reviews app is installed, configured, and approved submissions are published.</p>"
          }
        },
        {
          "type": "reviews_faq",
          "settings": {
            "question": "Can I leave a review?",
            "answer": "<p>Review collection is managed by your configured reviews app and store policies.</p>"
          }
        }
      ]
    }
  ]
}
{% endschema %}
